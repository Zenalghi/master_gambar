import 'package:flutter/material.dart';

class MasterVarianBodyScreen extends StatelessWidget {
  const MasterVarianBodyScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Halaman Master Varian Body'));
  }
}
