import 'package:flutter/material.dart';

class MasterMerkScreen extends StatelessWidget {
  const MasterMerkScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Halaman Master Merk'));
  }
}
