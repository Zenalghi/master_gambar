import 'package:flutter/material.dart';

class MasterTypeChassisScreen extends StatelessWidget {
  const MasterTypeChassisScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Halaman Master Type Chassis'));
  }
}
