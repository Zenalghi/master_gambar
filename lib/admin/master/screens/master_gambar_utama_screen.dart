import 'package:flutter/material.dart';

class MasterGambarUtamaScreen extends StatelessWidget {
  const MasterGambarUtamaScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Halaman Master Gambar Utama'));
  }
}
