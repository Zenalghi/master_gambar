import 'package:flutter/material.dart';
class MasterTypeEngineScreen extends StatelessWidget {
  const MasterTypeEngineScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Halaman Master Type Engine'));
  }
}